# Session Log — 2026-03-28

## Research

Searched for PI Web API endpoint coverage, competing SDKs, and common pain points:

- **Official AVEVA PI Web API Reference**: docs.aveva.com (dynamic loading blocks scraping)
- **Competing SDKs**: PI-Web-API-Client-Python (Swagger-generated, 30+ controllers), PIconnect, pi-web-sdk
- **Swagger-generated SDK**: Confirmed CalculationApi (get_at_intervals, get_at_recorded, get_summary, get_at_times) as high-impact gap
- **Previous session backlog**: Reviewed session-2026-03-26 prioritized suggestions
- **Specialist agents**: PI System Expert, Code Reviewer, Security Engineer consulted in parallel

## Gaps Found

### Implemented This Session
1. **Bug: `extra_fields` override order in `create_point`** — `body.update(extra_fields)` let extra_fields silently override standard params like `Name`
2. **Calculation controller** — server-side expression evaluation (summary, recorded, intervals, at-times) — highest-impact missing feature for data scientists
3. **Indexed AF Search** — `GET /search/query` for fast full-text AF object search

### Remaining Gaps (for future sessions)
1. `GET /streamsets/summary` — multi-stream summaries in one call
2. `POST /streamsets/value` and `POST /streamsets/recorded` — bulk multi-tag writes
3. `GET /points/{webId}/attributes` — PI point configuration attributes (Span, Zero, CompDev)
4. `boundaryType` and `includeFilteredValues` params on `get_recorded`
5. `selectedFields` param across all endpoints
6. `PATCH /elements/{webId}` — update element
7. Notification rules management
8. Security identity/mapping endpoints
9. Double error-handling cleanup (event hooks AND mixin methods both call raise_for_response)
10. WebID validation before URL construction
11. `GET /system/status`, `/system/userinfo`, `/system/versions` — system health checks

## Implemented This Session

### 1. Bug Fix: `extra_fields` override order in `create_point`
- Changed from `body.update(extra_fields)` (extra_fields wins) to building body from extra_fields first then applying standard params on top (standard params win)
- Applied to both sync and async `create_point` methods
- Added test verifying standard params cannot be overridden by extra_fields
- 1 new test

### 2. Feature: Calculation resource (`client.calculation`)
- New `calculation.py` with `CalculationMixin` + `AsyncCalculationMixin`
- Methods:
  - `get_recorded(expression, ...)` — evaluate expression at recorded event times
  - `get_summary(expression, ...)` — compute summary statistics of an expression
  - `get_at_intervals(expression, ..., sample_interval)` — evaluate at regular intervals
  - `get_at_times(expression, times, ...)` — evaluate at specific timestamps
- All methods support optional `web_id` parameter for context (relative tag references like `'.'`)
- Wired into `PIWebAPIClient` and `AsyncPIWebAPIClient` as `client.calculation`
- 17 tests covering happy paths, parameter forwarding, web_id omission, error handling, and all 4 async methods

### 3. Feature: Indexed AF Search (`client.search`)
- New `search.py` with `SearchMixin` + `AsyncSearchMixin`
- `query(q, *, scope, fields, max_count, start_index)` method calling `GET /search/query`
- Supports PI AF indexed search syntax: `name:Pump*`, `templateName:Equipment`, boolean operators
- New `SearchResult` class with `web_id`, `name`, `path`, `item_type`, `score`, `raw` attributes
- `SearchResult` exported from `__init__.py`
- Wired into both client classes as `client.search`
- 12 tests covering happy paths, parameter forwarding, empty results, errors, defaults, and repr

## Test Counts
- Before: 377 passed
- After: 407 passed (+30 new tests)
- Lint: 0 errors (ruff)
- Mypy: 0 errors (strict mode, 20 source files)

## Pushed
- 3 commits pushed to `master`:
  - `fix: prevent extra_fields from overriding standard params in create_point`
  - `feat: add Calculation resource for server-side expression evaluation`
  - `feat: add indexed AF Search resource (GET /search/query)`

## Suggested for Next Session (prioritized)

### Quick Wins (parameter additions)
1. **`boundaryType` param on `get_recorded`** — controls boundary interpolation ("Inside", "Outside", "Interpolated")
2. **`includeFilteredValues` param on `get_recorded`** — companion to filterExpression
3. **`selectedFields` param** across all GET endpoints — reduce payload size

### New Endpoints (high impact)
4. **Streamset bulk writes** — `POST /streamsets/value` and `POST /streamsets/recorded`
5. **System resource** — `GET /system/status`, `/system/userinfo`, `/system/versions` for health checks
6. **Streamsets summary** — `GET /streamsets/summary` for multi-stream summaries
7. **Point config attributes** — `GET /points/{webId}/attributes` (Span, Zero, CompDev, etc.)
8. **Update element** — `PATCH /elements/{webId}`

### Quality & Consistency
9. **Resolve double error-handling** — event hooks AND mixin methods both call raise_for_response; pick one
10. **WebID validation** — reject empty/None WebIDs before URL construction
11. **Notification rules resource** — models exist but no resource methods
